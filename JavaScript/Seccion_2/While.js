// while - Recorre un bloque de código mientras se cumpla una la condicion indicada.
let vueltas = 5;
let repeticiones = 1;

// While / Mientras

while( repeticiones < 5 ){
    console.log(repeticiones);
    repeticiones++;
}

repeticiones;



// Do While / Hacer Mientras
let ciclos = 5;
let numCiclos = 1;

do{
    console.log(numCiclos);
    numCiclos++;
}while( numCiclos <= ciclos );